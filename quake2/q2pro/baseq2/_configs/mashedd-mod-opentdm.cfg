// Show weapons HUD
set uf "256" u

// Make enemy "male" model, doesn't work when reconnecting to match
trigger "eskin male/grunt" "*${name} is ready!*"

// HUD Timers
set amask "+all -ga" u
set wmask "-all +cg +rl +rg +bfg" u

// Auto ready
//trigger "ready" "*is ready!*"

