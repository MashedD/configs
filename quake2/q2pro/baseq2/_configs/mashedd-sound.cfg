//set rate "25000"
//set s_volume "0.7"
set s_mixahead "0.05" // not used with OpenAL
set ogg_enable "0" // 1 by default in q2repro, 0 disables music

set s_enable "2" // OpenAL
//set s_ear_midon "1" // make sound more like in r1q2
set s_underwater "0" // hearing underwater should be improved
//set s_underwater_gain_hf "1.0" // in case s_underwater is set to 1, less sounds should be cut off

set s_ambient "3"
//set s_auto_focus "2" // sound is activated when main window has input focus, and deactivated when it loses it

